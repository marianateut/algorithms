package org.fastrackit;

public class Diferenta {

    public int mathdif (int a, int b) {
        int dif = a - b;
        System.out.println("Diferenta numerelor " + a + " si " + b + " este " + dif);
        return dif;
    }

    public double mathdif (int a, double b) {
        double dif = a - b;
        System.out.println("Diferenta numerelor " + a + " si " + b + " este " + dif);
        return dif;
    }

    public double mathdif (double a, int b) {
        double dif = a - b;
        System.out.println("Diferenta numerelor " + a + " si " + b + " este " + dif);
        return dif;
    }

    public double mathdif (double a, double b) {
        double dif = a - b;
        System.out.println("Diferenta numerelor " + a + " si " + b + " este " + dif);
        return dif;
    }
}


